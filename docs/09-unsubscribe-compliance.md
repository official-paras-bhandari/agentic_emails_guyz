# Unsubscribe and suppression

Every draft receives an opt-out footer. An unsubscribe request creates a workspace suppression entry, marks matching leads unsubscribed, cancels pending sends, and cancels scheduled follow-ups in one backend operation.

Suppression checks cover normalized email and exact normalized domain. Removing a suppression entry is an explicit confirmed action and does not erase historical lead or audit state.
