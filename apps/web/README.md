# Web application

This is the Next.js UI and API for Agentic Outreach. Run it from the repository root with `npm run dev:web`; use `npm run dev:all` for the complete PostgreSQL, Redis, web, worker API, and RQ stack.

Configuration, production requirements, and acceptance-test commands are documented in the repository root [README](../../README.md).
